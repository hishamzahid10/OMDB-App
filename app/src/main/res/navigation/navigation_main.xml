<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph.xml"
    app:startDestination="@id/movie_fragment">

    <fragment
        android:id="@+id/movie_fragment"
        android:name="com.hisham.movie.ui.ui.home.movie.feed.MovieFragment"
        android:label="Feed"
        tools:layout="@layout/movie_fragment">
        <action
            android:id="@+id/action_movie_fragment_to_movie_detail_fragment"
            app:destination="@id/movie_detail_fragment"
             />
    </fragment>

    <fragment
        android:id="@+id/movie_detail_fragment"
        android:name="com.hisham.movie.ui.ui.home.movie.feed.MovieDetailFragment"
        android:label="Detail"
        tools:layout="@layout/movie_detail_fragement"/>

</navigation>
